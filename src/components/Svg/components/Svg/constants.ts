import { type IRectangle } from '../..'

export const defaultViewBox: IRectangle = { x: 0, y: 0, width: 100, height: 100 }

export const defaultPosition = { x: 0, y: 0 }
export const defaultPositionCentered = { x: '50%', y: '50%' }
